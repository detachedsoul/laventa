import BlogWrapper from "@components/blog/BlogWrapper";

export const metadata = {
	title: "Laventa | Blog Posts",
	description: "View different blog posts"
};

const Page = () => {
    return (
		<BlogWrapper />
	);
};

export default Page;
